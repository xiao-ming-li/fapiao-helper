# 发票 PDF 合并 Skill

## 目标

把原始发票目录中的 PDF 和图片按处理顺序合并为一个附件 PDF。

## 规则

- PDF 原页必须完整插入，不重新排版。
- 图片发票按 A4 页面等比居中放置。
- 非正式发票附件也应合并进 PDF，保证报销凭证链完整。
- 生成的 `发票附件_*.pdf` 不应在下次运行时再次作为原始发票读取。
- 如果单个文件无法合并，应在附件 PDF 中插入错误说明页，而不是中断整个批次。
